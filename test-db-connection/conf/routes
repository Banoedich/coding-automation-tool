# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                               controllers.HomeController.index()
# POST  /submit                         controllers.HomeController.submit
GET     /form                           controllers.HomeController.get_form()
GET     /getFormData                    controllers.HomeController.getFormData(name, price: Int)
# В отличии от GET-запроса при POST не нужно передавать аргументы, чтобы данные не были в URL-адресе

POST     /postFormData                     controllers.HomeController.postFormData
POST     /validateCoffeeForm             controllers.HomeController.validateCoffeeForm

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
